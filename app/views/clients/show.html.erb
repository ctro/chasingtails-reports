<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @client.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @client.email %>
</p>

<p>
  <strong>Address:</strong>
  <%= @client.address %>
</p>

<p>
	<strong>Dogs:</strong>
	<% @client.dogs.each do |dog| %>
		<%= dog.name %><br/>
	<% end %>
</p>

<%= link_to 'Edit', edit_client_path(@client) %> | 
<%= link_to 'Destroy', @client, method: :delete, data: { confirm: 'Are you sure?' } %>
<br/>
<%= link_to 'Dogs', client_dogs_path(@client) %>
<br/>
<%= link_to 'Back', clients_path %>
